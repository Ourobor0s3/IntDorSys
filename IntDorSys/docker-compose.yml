services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: intdorsys.web.api
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Development
      ConnectionStrings__Battle: "Host=host.docker.internal;Port=5433;Database=kai_dorm;Username=postgres;Password=admin"